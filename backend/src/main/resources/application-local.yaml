spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/petmatch_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:duymac}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

app:
  seed:
    enabled: false

cloudinary:
  cloud-name: test
  api-key: test
  api-secret: test

openai:
  api-key: test
